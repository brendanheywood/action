name: ci
on: [push]
jobs:
  check-bats-version:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: echo "hello world"
      - run: |
            echo "::group::my title"
            echo "inside group"
            echo "::endgroup::"
      - run: |
            echo "::group::my group with an error"
            echo "::error file=README.md,line=2,col=5,endColumn=7::Missing semicolon"
            echo "::endgroup::"
